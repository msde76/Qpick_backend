spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/qpick?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: thdtjdghks2@

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  # Redis & Kafka 설정
  # data:
  #   redis:
  #     host: localhost
  #     port: 6379

  ai:
    openai:
      api-key: sk-dummy-key-for-startup

logging:
  level:
    org.hibernate.SQL: debug